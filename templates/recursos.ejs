<!DOCTYPE html>
<html lang="es">
<head>
  <% include service/partials/head.html %>
</head>
<body>
  <!-- Header -->
  <% include service/partials/header.html %>

  <!-- Banner específico de página -->
  <% include service/partials/banner.html %>

  <!-- Contenido principal -->
  <main id="main-content" class="container">
    <section id="recursos" class="content-section" role="region" aria-labelledby="recursos-title" data-aos="fade-up">
      <h1 id="recursos-title" class="section-title">Recursos & Comunidad</h1>
      <p>En esta sección podrás compartir enlaces, documentos y todo tipo de recursos útiles para la comunidad de Nubia.</p>
      <!-- Ejemplo de lista de recursos:
      <ul>
        <% resources.forEach(r => { %>
          <li><a href="<%= r.url %>"><%= r.name %></a></li>
        <% }) %>
      </ul>
      -->
    </section>
  </main>

  <!-- Footer -->
  <% include service/partials/footer.html %>
  <!-- Chat widget -->
  <% include service/partials/chat.html %>
  <!-- Scripts JS -->
  <% include service/partials/scripts.html %>
</body>
</html>

