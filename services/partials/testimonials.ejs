<section class="testimonials" data-aos="fade-up" role="region" aria-labelledby="testimonials-title">
  <div class="container">
    <h2 id="testimonials-title" class="section-title">Testimonios</h2>
    <div class="testimonial-grid">
      <% testimonials.forEach(testimonial => { %>
        <%- include('testimonial.ejs', { testimonial: testimonial }) %>
      <% }) %>
    </div>
  </div>
</section>

